interface:
  display_name: "Build Release"
  short_description: "Publish env-gradoom-turbo-torch releases"
  default_prompt: "Use $build-release to publish the next env-gradoom-turbo-torch version and verify it on PyPI."
